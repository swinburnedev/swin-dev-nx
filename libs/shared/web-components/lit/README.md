# shared-web-components--lit

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test shared-web-components--lit` to execute the unit tests via [Jest](https://jestjs.io).
