import { sharedWebComponentsLit } from './shared-web-components--lit';

describe('sharedWebComponentsLit', () => {
  it('should work', () => {
    expect(sharedWebComponentsLit()).toEqual('shared-web-components--lit');
  });
});
